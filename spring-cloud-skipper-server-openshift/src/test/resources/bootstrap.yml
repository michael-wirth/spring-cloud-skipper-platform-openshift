spring:
  application:
    name: spring-cloud-skipper-server-openshift

  cloud:
    config:
      enabled: false
    skipper:
      server:
        enableLocalPlatform: false
        platform:
          openshift:
            accounts:
              minikube:
                namespace: scdf
